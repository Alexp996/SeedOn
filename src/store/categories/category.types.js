

export const CATEGORIES_ACTION_TYPES= {
    SET_CATEGORIES: 'SET_CATEGORIES'
}

/** we create this key with the same name as value, 
 * because we want to acces this in category.reducer as a type in switch(type) 
 * and the if the type match the case, return an object, there in category.reducer
 *  
 * 
 */